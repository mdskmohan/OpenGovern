# OpenGovern Ingestion Worker dependencies
#
# opengovern-connectors is our fork of acryl-datahub's metadata-ingestion.
# It is vendored directly in this repo under ./opengovern_connectors/
# Zero runtime dependency on acryl-datahub or DataHub servers.
#
# To sync latest connector code from DataHub upstream:
#   ./scripts/sync-from-datahub.sh

# ── OpenGovern connector package (local vendored fork) ───────────────────────
-e ./opengovern_connectors[all]

# ── Worker runtime ────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
psycopg2-binary>=2.9
httpx>=0.25
pydantic>=2.0
pydantic-settings>=2.0
PyYAML>=6.0
python-dotenv>=1.0
click>=8.1
rich>=13.0
