# Core framework
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.25
sqlglot>=20.0
click>=8.1
rich>=13.0
PyYAML>=6.0
python-dotenv>=1.0
psycopg2-binary>=2.9
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# DataHub ingestion — ALL 50+ connectors pre-installed
# Users never need to run pip install manually
acryl-datahub[snowflake,bigquery,redshift,postgres,mysql,mssql,oracle,trino,athena,mariadb,teradata,vertica,mongodb,cassandra,clickhouse,db2,elasticsearch,databricks,hive,presto,dbt-cloud,airflow,dagster,prefect,looker,tableau,powerbi,metabase,superset,kafka,pulsar,s3,gcs,spark,flink,glue,mlflow,sagemaker,feast,great-expectations,delta-lake]>=0.12.0
