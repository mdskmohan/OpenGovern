# OpenGovern Ingestion Worker
# Uses acryl-datahub connectors directly.
# Fork to opengovern-connectors is a separate milestone tracked in .github/workflows/sync-connectors.yml

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
psycopg2-binary>=2.9
httpx>=0.25
pydantic>=2.0
pydantic-settings>=2.0
PyYAML>=6.0
python-dotenv>=1.0
click>=8.1
rich>=13.0

# DataHub connectors — all major platforms pre-installed, no manual pip install needed
acryl-datahub[snowflake,bigquery,redshift,postgres,mysql,dbt-cloud,airflow,looker,tableau,kafka,s3,databricks,hive,clickhouse,powerbi,metabase]>=0.12.0
